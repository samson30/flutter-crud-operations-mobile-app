import 'package:flutter/material.dart';
// used common page for common used colors in the app

class AppColors {
  static const Color primary = Color(0xFF00695C); // Teal
  static const Color accent = Color(0xFF26A69A); // Lighter Teal
  static const Color background = Color(0xFFF5F5F5); // Light Grey
  static const Color textPrimary = Color(0xFF212121); // Dark Text
  static const Color textSecondary = Color(0xFF757575); // Light Text
  static const Color inputBoxBorderColor = Color.fromARGB(255, 202, 200, 200);
  static const Color errorColor = Color(0xFFD32F2F); // Red for buttons
  static const Color lightText = Color.fromARGB(255, 255, 255, 255);
  static const toastSuccess = Colors.green;
  static const toastText = Colors.white;
  static const disabledColor = Color.fromARGB(255, 231, 228, 228);
}
